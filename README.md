# eBackup

> 🌐 **Languages:** **Русский** (этот файл) · [English](README.en.md)

**eBackup** — модульное приложение для Windows, которое бэкапит настройки приложений
и любые ваши папки, хранит архивы сразу в нескольких местах (локально, SFTP) и
аккуратно раскладывает всё обратно — даже на другом компьютере или после
переустановки ОС. Тёмный фирменный интерфейс + полноценный CLI.

Бесплатно и без подписок — делаем для людей.

> 🚧 Проект в активной разработке, но **полный цикл уже работает**:
> бэкап → хранение → восстановление → удаление, и из GUI, и из CLI.

## Возможности

**Интерфейс (WinUI 3, тёмная тема):**
- 🚀 **Бэкап** — выбор модулей (OBS Studio) и **любых своих папок**, несколько
  целей сразу (локально + серверы), опциональное шифрование, живой прогресс
- 🌐 **Хранилища** — SFTP-подключения: приватный ключ вставляется **содержимым**
  и хранится зашифрованным (файл ключа не обязан лежать на диске), кнопка «Тест»,
  автопроверка доступности (✓/✕ у каждого подключения), выбор удалённой папки
  **деревом** с ленивой подгрузкой
- 📦 **Архивы** — локальные и серверные списки (размер, дата, 🔒 у зашифрованных),
  восстановление и удаление по кнопке
- ♻️ **Восстановление** — в исходные места **или в любую указанную папку**;
  режимы конфликтов (заменить с `.bak` / перезаписать / только недостающие);
  внешние ассеты сцен OBS раскладываются в выбранную папку, а пути в сценах
  переписываются автоматически
- ⚙️ **Настройки** — папка локальных бэкапов и **хранение последних N версий**
  (старые архивы чистятся автоматически и локально, и на серверах)

**CLI** — всё то же самое скриптуемо: `backup`, `restore`, `sftp-add/list/test/ls`,
`module-add`; парольная фраза для автоматизации — через переменную окружения
`EBACKUP_PASSPHRASE`.

**Модули:**
- 🎬 **OBS Studio** (встроенный): конфигурация без кэшей и логов, плагины из
  папки установки, и **зависимые ассеты сцен** (фоны-картинки, видео и т.п.,
  лежащие вне OBS) — с переписыванием путей при восстановлении
- 🧩 **Декларативные drop-in модули**: положите `*.module.json` в
  `%APPDATA%\eBackup\modules` (или `ebackup module-add файл`) — и приложение
  начнёт бэкапить описанные пути. Без кода и пересборки.

```json
{
  "id": "myapp",
  "displayName": "My App",
  "entries": [
    { "tokenPath": "{APPDATA}/MyApp", "type": "Directory",
      "archivePath": "config", "excludeGlobs": ["**/Cache/**"] }
  ]
}
```

**Безопасность:**
- 🔐 Опциональное шифрование архива: **AES-256-GCM**, ключ из парольной фразы
  через **Argon2id** — безопасно класть на чужие серверы
- 🔑 Секреты подключений (пароли, ключи) — через **Windows DPAPI**, в конфиге
  в открытом виде не лежат и намеренно **не переносятся** на другой ПК
- 🛡️ Декларативным модулям разрешены только app-data пути (никаких
  `.ssh`/ключей/Program Files), архивы проверяются от path-traversal

**Формат архива `.ebk`** — ZIP + `manifest.json` с токенизированными путями
(`{APPDATA}` и т.п.), поэтому архив **переносим между компьютерами**. Имена
говорящие и сортируемые: `ebackup_obs-folders_2026-06-10_01-55-27.ebk`.

## Сборка и запуск

Требуется [.NET SDK 9.0+](https://dotnet.microsoft.com/download) (Windows 10/11).

```powershell
dotnet build eBackup.sln
dotnet test  eBackup.sln

# GUI
.\src\eBackup.App\bin\x64\Debug\net9.0-windows10.0.19041.0\win-x64\eBackup.App.exe

# CLI
dotnet run --project src/eBackup.Cli -- list-modules
dotnet run --project src/eBackup.Cli -- backup --out .\backups --encrypt
dotnet run --project src/eBackup.Cli -- restore --archive .\backups\<имя>.ebk --to C:\Temp\check
```

## Архитектура

| Проект | Назначение |
|---|---|
| `eBackup.Abstractions` | Контракт плагинов (интерфейсы, модель манифеста) — стабильная граница |
| `eBackup.Core` | Движок бэкапа/восстановления, шифрование, реестр модулей, декларативные модули |
| `eBackup.Security` | Защита секретов через Windows DPAPI |
| `eBackup.Storage.Local` / `.Sftp` | Хранилища: локальная папка и SFTP (SSH.NET) |
| `eBackup.Modules.Obs` | Модуль OBS Studio (конфиг + плагины + ассеты сцен) |
| `eBackup.App` | GUI на WinUI 3 |
| `eBackup.Cli` | Консольный интерфейс |
| `eBackup.Tests` | Тесты |

Подробности — в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

## Дорожная карта
- Экран «Модули» в GUI (карточки реестра, импорт)
- Живой дашборд на «Обзоре»
- Иконка в трее и расписание автоматических бэкапов
- Бэкап ключей реестра Windows; новые модули приложений
- Хранилища FTP и Google Drive
- Динамические DLL-плагины (с моделью доверия и подписями)
- Установщик одним exe; анимации интерфейса; логотип

## Лицензия
[MIT](LICENSE) © Erney (erneywhite)
